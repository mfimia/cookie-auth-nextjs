import { NextPage } from "next";
import { Fragment } from "react";

const Login: NextPage = () => {
  return (
    <Fragment>
      <div className="jumbotron text-center bg-primary square">Login</div>
      <p>login page</p>
    </Fragment>
  );
};

export default Login;
